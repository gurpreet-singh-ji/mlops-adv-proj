apiVersion: v1
kind: Service
metadata:
  name: mlops-app-service
spec:
  type: LoadBalancer
  selector:
    app: mlops-app
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000